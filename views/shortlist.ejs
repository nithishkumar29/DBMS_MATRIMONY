<!DOCTYPE html>
<html>
<head>
    <title>Shortlists</title>
	<link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css">
	<script src="https://ajax.googleapis.com/ajax/libs/jquery/3.3.1/jquery.min.js"></script>
	<script src="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/js/bootstrap.min.js"></script>
    <link href="https://fonts.googleapis.com/css?family=Montserrat" rel="stylesheet" type="text/css">
    <link href="https://fonts.googleapis.com/css?family=Lato" rel="stylesheet" type="text/css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/4.7.0/css/font-awesome.min.css">
    <link href="https://fonts.googleapis.com/css?family=Krub:300,400" rel="stylesheet">
    <link href="https://fonts.googleapis.com/icon?family=Material+Icons" rel="stylesheet">
    <script src="https://ajax.googleapis.com/ajax/libs/angularjs/1.6.9/angular.min.js"></script>
    <style type="text/css">
        body{
            background-color: white;
            background-image: url("/images/Dark-color-wood-texture-background-vector-09.jpg");
        }
    	#header{
               position:fixed;
               top:0%;
               width:100vw;
               height:10%;
               background:linear-gradient(to right, #3b3c3d,#6d6e70);
               border:1px solid white;
               overflow: hidden;
               }
        #header img{
        float: left;
        width: 150px;
        height: 78%;
        background: #555;
        cursor: pointer;
        margin-left: 1%;
        margin-top:0.5%;
        /*border-radius: 15px;*/
        } 
         #header #username{
            font-family: 'Krub', sans-serif;
            position:absolute;
            color:#ff6600;
            background-color:white;
            left:80%;
            top:20%;
            bottom:20%;
            border: 2px lightgrey;
            font-size:25px;
            background-color: #6d6e70;
            justify-content:center;
            border-radius: 10px;
            padding:2px 4px;
        }
        #username p{
            justify-content:center;
            border-radius: 5px;
        }
        #username p:hover{
            color: red;
        }
        #eachContainer{
            position: static;
            margin-top: 10%;
            margin-left: 10%;
            margin-right: 20%;
            margin-bottom: -10%;
            width:80%;
            padding: 20px 20px 20px 20px;
            padding-left: 35px;
            padding-top: 35px;
        }
        #pInfo{
            float: right;
            border: 2px solid grey;
            margin-top: -235px;
            margin-left: -40px;
            position: static;
            padding-top: 15px;
            padding-bottom: 10px;
            padding-left: 25px;
            padding-top: 10px;
            width:60%;
            height:220px;
            background-color: lightgrey;
            align-items: center;
            margin-right: 130px;
            border-radius: 20px;
        }
        p
        {
            font-family: 'Krub', sans-serif;
            font-size: 15px;
            font-weight: bold;
        }
        #imgContainer{
            width:250px;
            height: 250px;
        }
        #imgContainer img{
            align-items: center;
            position: static;
            margin-left: 15px;
            margin-right: 15px;
            margin-top: 15px;
            margin-bottom: 15px;
            border-radius: 50%;
        }
        #btnSubmitlike{
            float: right;
            height: 110px;
            margin-top: -175px;
            margin-right: 50px;
            background-color: red;
            color:white;
        }
        #validbtnSubmitlove{
            position: static;
            float: right;
            height: 60px;
            margin-top: -15px;
            margin-right: 50px;
            background-color: black;
            color:white;
        }
        #deleteIcon
        {
            font-size: 48px;
        }
        .fa{
            font-size: 20px;
        }
        .fa-heart
        {
            font-size: 24px;
        }
        #btnSubmitlike:hover{
            background-color:white ;
            color:red;
        }
        #validbtnSubmitlove:hover{
            background-color:white ;
            color:black;
        }
        .imgFit{
            width:220px;
            height: 220px;
        }
        .mName{
            font-size: 20px;
        }
        #mname
        {
            color: green;
            font-weight: bold;
            font-size: 25px;
        }
        #mname:hover{
            cursor: pointer;
            text-decoration: underline;
        }
        .mGender
        {
            margin-left: 15px;
        }
        #mAge
        {
            margin-left: 15px;
        }
        #gIcon
        {
            margin-left: 5px;
        }
        #err{
            font-weight: bold;
            font-size: 40px;
            color:lightgrey;
            margin-top: 100px;
            margin-left: 0px;
            text-align: center;
        }
        #home{
            position: absolute;
            left:90%;
            top:2.5%;
            width:5%;
            height:5%;
            color:white;
            font-size:20px;
            cursor: pointer;
            border:1px solid cyan;
            
            justify-content:center;
        }
        #homeTxt{
            position:absolute;
            top:4%;
            left:15%;
            font-size:20px;
            letter-spacing:2px;
            font-family:monospace;
            font-weight:800;
            color: white;
        }
        #homeIcon{
            position:absolute;
            top:20%;
            left:35%;
            color:cyan;
        }
    </style>
</head>
<body>
    <div id="header">
        <img src="/images/logo2.png" alt="logos" onclick="location.href='/dashboard' " title="home page" > 
        <span  id="username"><p style="font-size:25px;"> Hi, <%= user.pname %> </p></span>
    </div>
    <div id="btn">
        <form id="deleteShortlist" action="/shortList/delete" method="POST">
        <button name="delete" id="validbtnSubmitlove" class="validLove" type="submit"title="Clear Shortlist"><p title="Clear Shortlist"><i class="material-icons" id="deleteIcon">delete</i></p></span></button>
        </form>
    </div>
    <div id="pContainer" >
        <% if( members.length > 0) { %>
            <% for( var i=0; i< members.length; i++ ) { %>
                <div id="eachContainer">
                    <form id="<%= members[i].pname %>" action="/shortList/like" method="POST">
                    <div id="imgContainer" align="left">
                        <% if (members[i].image) { %>
                        <img class="imgFit" src="/userImages/<%= members[i].pname %>.jpg">
                        <% } else { %>
                        <img class="imgFit" src="/images/images_5.jpg" alt="Upload image">
                        <% } %>
                    </div>
                    <div id="pInfo" class="userInfo" >
                        <p class="mName"><span id="mname" onclick="location.href='http://localhost:8081/dashboard/userProfile?partner=<%= members[i].pname %>'"><%= members[i].pname %></span><span class="mGender"> <%= members[i].gender %><i class="fa fa-<%= members[i].gender %>" id="gIcon"></i></span>
                        <%
                        function calcAge(dateString) {
                          var birthday = new Date(dateString);
                          var ageDifMs = Date.now() - birthday.getTime();
                          var ageDate = new Date(ageDifMs); // miliseconds from epoch
                          return Math.abs(ageDate.getFullYear() - 1970);
                        } 
                        function toFeet(n) {
                          var realFeet = ((n*0.393700) / 12);
                          var feet = Math.floor(realFeet);
                          var inches = Math.round((realFeet - feet) * 12);
                          return feet + "\'" + inches;
                        }
                        function shortListed(shortlistedArray,str) {
                          return (shortlistedArray.indexOf(str) > -1);
                        }
                        %>
                        <span id="mAge"><%= calcAge(members[i].DOB) %> Years </span></p>
                        <p class="mReligion">
                            <span class="rKey">Religion : </span><span class="rValue"><%= members[i].religion %>, </span>
                            <span class="hKey">Height : </span><span class="hValue"><%= toFeet(members[i].userHeight) %></span>
                        </p>
                        <p class="mEdu">
                            <span class="mKey">Education : </span><span class="mValue"><%= members[i].qualification %> </span>
                        </p>
                        <p class="mOccupation">
                            <span class="mKey">Occuption : </span><span class="mValue"><%= members[i].occupation %> </span>
                        </p>
                        <p class="mStatus">
                            <span class="mKey">Marital Status : </span><span class="mValue"><%= members[i].mStatus %> </span>
                        </p>
                        <p class="mCollege">
                            <span class="mKey">Studied at </span><span class="mValue"><%= members[i].college %> </span>
                        </p>
                    </div>
                    <button name="like" id="btnSubmitlike" type="submit" value="<%= members[i].pname %>" title="Interested"><span>
                    <i class="fa fa-heart"></i></span></button>
                    </form>
                </div>
            <% } %>
        <% } else { %>
            <p id="err"> No Profiles Shortlisted </p>
        <% } %>
    </div>
     <div id="home" onclick="location.href='/dashboard' ">
        <p id="homeTxt">Home</p><p><span id="homeIcon" class="glyphicon glyphicon-home"></span></p>
    </div>
</body>
<script type="text/javascript">
    $(document).ready(function(){
       $("#homeIcon").hide();
           $("#home").hover(function(){
                $("#homeTxt").hide();
                $("#homeIcon").show();
           },
              function(){
              $("#homeTxt").show();
                $("#homeIcon").hide();
           });
    });
</script>
</html>